import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Sohail Bagali · DevOps & Cloud Engineer",
  description:
    "Portfolio of Sohail Bagali — DevOps & Cloud Engineer focused on AWS, Kubernetes, CI/CD, IaC, and observability.",
  openGraph: {
    title: "Sohail Bagali · DevOps & Cloud Engineer",
    description:
      "Building scalable cloud infrastructure, CI/CD pipelines, containerized deployments, and automation workflows.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`dark ${geistSans.variable} ${geistMono.variable} h-full scroll-smooth antialiased`}
    >
      <body className="flex min-h-full flex-col font-sans">{children}</body>
    </html>
  );
}
